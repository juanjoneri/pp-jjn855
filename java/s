#!/bin/bash

# NOTE: do NOT modify this file.  This script runs tests.

set -e

echo "Compile code"
( cd code
  javac *.java )

( cd code
  ./simple -update -header "0,0,__sum__([1,0],[1,1])" ../tests/0.txt ../tests/0.out )
