#!/bin/bash

# NOTE: do NOT modify this file.  This script runs tests.

set -e

echo "Compile code"
( cd code
  javac *.java )

( cd code
  ./simple 0,1 ../tests/0.txt -header ../tests/0.out -print )
