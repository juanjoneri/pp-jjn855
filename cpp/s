#!/bin/bash

# NOTE: do NOT modify this file.  This script runs tests.

set -e

echo "Compile code"
( cd code
  g++ -std=c++11 *.cpp )

echo "Running code"
( cd code
  ./simple -print 0 ../tests/1.txt ../tests/1.out )
